// postcss.config.js
// With @tailwindcss/vite, this file might be very simple or even unnecessary
// if Vite handles PostCSS internally and @tailwindcss/vite injects what's needed.
// For now, keeping it minimal. If other PostCSS plugins are needed, they can be added here.
export default {
  plugins: {
    // tailwindcss: {}, // Handled by @tailwindcss/vite
    // autoprefixer: {}, // Handled by @tailwindcss/vite
  },
};
