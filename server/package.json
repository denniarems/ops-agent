{
  "name": "server",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "dev": "wrangler dev",
    "build": "wrangler deploy --minify",
    "start": "bun run src/index.ts",
    "cf-typegen": "wrangler types --env-interface CloudflareBindings"
  },
  "dependencies": {
    "hono": "^4.7.11",
    "ofetch": "^1.4.1",
    "zod": "^3.25.62",
    "shared": "workspace:*",
    "agent": "workspace:*"
  },
  "devDependencies": {
    "wrangler": "^4.4.0",
    "@types/bun": "latest",
    "typescript": "^5.8.3"
  }
}